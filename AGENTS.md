# Agents

## Code Style

Functional, declarative. Prefer `map`/`find`/`filter` pipelines over imperative loops. Small composable helpers. Expression-oriented.

## Validation

```sh
bun run build
bun test
```
